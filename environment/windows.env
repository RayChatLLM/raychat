# Default settings: ~/.raychat/environment/.env (configured application storage).
# Portable mode: copy to environment/.env and launch with --portable.
# Or choose --env-file PATH. Quotes are optional; no shell sourcing is needed.
RAYCHAT_AUTH_TOKEN=
RAYCHAT_MODEL=
RAYCHAT_BASE_URL=
