# Copy to environment/.env and fill in these three values.
# RayChat loads environment/.env automatically; no shell commands are needed.
# Quotes are optional. Keep credentials in the Git-ignored .env, not this example.
RAYCHAT_AUTH_TOKEN=
RAYCHAT_MODEL=
RAYCHAT_BASE_URL=
