# Copy to .env, fill in these three values, then load with:
# set -a; . ./.env; set +a
# Keep the single quotes around values. Never commit your filled-in .env.
RAYCHAT_AUTH_TOKEN=''
RAYCHAT_MODEL=''
RAYCHAT_BASE_URL=''
